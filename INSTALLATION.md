# 📦 Инструкция по установке для коллег

## Вариант 1: Через Git (лучший способ) ⭐

1. Загрузите проект на GitHub/GitLab
2. Коллеги устанавливают одной командой:

**Через HTTPS:**
```bash
pip install git+https://github.com/AlexMayka/logging_python.git
```

**Через SSH (рекомендуется):**
```bash
pip install git+ssh://git@github.com/AlexMayka/logging_python.git
```

💡 Для SSH требуется настроенный SSH ключ в GitHub

---

## Вариант 2: Локальная установка из папки

### Для того, кто делится:
1. Заархивируйте всю папку `logging_python/`
2. Отправьте коллеге (по почте, Slack, и т.д.)

### Для коллеги, который устанавливает:
```bash
# Распаковать архив
unzip logging_python.zip
cd logging_python

# Установить
pip install .
```

---

## Вариант 3: Прямая установка (самый простой)

### Для того, кто делится:
Просто отправьте папку `logging_python/` коллеге

### Для коллеги:
```bash
# Перейти в папку с проектом
cd /путь/к/папке/logging_python

# Установить в режиме разработки (изменения сразу доступны)
pip install -e .

# ИЛИ установить обычным способом
pip install .
```

---

## Вариант 4: Ручное копирование (без установки)

### Для коллеги:
Просто скопируйте папку `log/` в свой проект:

```
your_project/
├── log/              # ← скопированная папка
│   ├── __init__.py
│   └── log.py
└── your_script.py
```

И используйте:
```python
from log import Log

logger = Log(token="ваш-токен")
logger.info("Привет!")
```

---

## Вариант 5: Внутренний PyPI сервер (для компании)

Если у вас есть внутренний PyPI:

```bash
# Создать дистрибутив
python setup.py sdist bdist_wheel

# Загрузить на внутренний PyPI
twine upload --repository-url https://your-pypi-server dist/*

# Коллеги устанавливают
pip install poradock-logging --index-url https://your-pypi-server
```

---

## ✅ Проверка установки

После установки любым способом, проверьте:

```python
from log import Log
print("✅ Библиотека установлена успешно!")
```

---

## 🔑 Токен

Не забудьте передать коллегам токен для API!

Токен: `f95e9305-107e-4967-ad3f-c65c70e8930a`

**⚠️ Важно:** Если токен приватный, передавайте через безопасные каналы!

